import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import './globals.css'

export const metadata: Metadata = {
    title: 'Pokemon Discovery',
 
  description: 'Discover Pokemon and build your personal collection',
  openGraph: {
    title: 'Pokemon Discovery',
    description: 'Discover Pokemon and build your personal collection',
    url: 'https://pokemon-discovery.vercel.app',
    siteName: 'Pokemon Discovery',
    images: [
      {
        url: 'https://pokemon-discovery.vercel.app/og-image.png',
        width: 1200,
        height: 630,
      },
    ],
    locale: 'en_US',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body>{children}</body>
    </html>
  )
}
